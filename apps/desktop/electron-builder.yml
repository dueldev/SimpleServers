appId: com.simpleservers.desktop
productName: SimpleServers
copyright: Copyright © 2026
asar: true
generateUpdatesFilesForAllChannels: true

directories:
  output: ../../release/desktop

publish:
  provider: github
  owner: charlesshaw3
  repo: SimpleServers
  releaseType: release

files:
  - dist/**
  - package.json

extraResources:
  - from: ../api/dist
    to: api
    filter:
      - "**/*"
  - from: ../web/dist
    to: web
    filter:
      - "**/*"

mac:
  category: public.app-category.developer-tools
  hardenedRuntime: false
  gatekeeperAssess: false
  target:
    - dmg
    - zip

win:
  target:
    - nsis

linux:
  maintainer: "SimpleServers contributors <charlesshaw3@users.noreply.github.com>"
  executableName: simpleservers
  artifactName: SimpleServers-${version}-${arch}.${ext}
  target:
    - AppImage
    - deb
