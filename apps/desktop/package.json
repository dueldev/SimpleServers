{
  "name": "@simpleservers/desktop",
  "version": "0.1.3",
  "private": true,
  "description": "SimpleServers desktop shell",
  "author": {
    "name": "SimpleServers contributors",
    "email": "charlesshaw3@users.noreply.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/charlesshaw3/SimpleServers.git"
  },
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint not configured yet'",
    "format": "echo 'format not configured yet'",
    "dist": "electron-builder --config electron-builder.yml --publish never",
    "dist:publish": "electron-builder --config electron-builder.yml --publish always"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "electron": "37.10.3",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.2"
  }
}
